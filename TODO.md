* allow parametrization for sheet in the spreadsheet file
* allow parametrization for refresh token secret name (to support multiple users)
* specify column order (with id first, timestamp second)
* write column headers at the top
* either apply cell formatting (e.g. second count as hour:minute:sec) or create additional preformatted columns
* propose summary formula (ride distance separate, walk distance separate)? or maybe unified value?
* plan a table with used points deduction
