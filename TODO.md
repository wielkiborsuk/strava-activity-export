* [X] allow parametrization for sheet in the spreadsheet file
* [X] allow parametrization for refresh token secret name (to support multiple users)
* [X] specify column order (with id first, timestamp second)
* [X] write column headers at the top
* [-] either apply cell formatting (e.g. second count as hour:minute:sec) or create additional preformatted columns
* [X] propose summary formula (ride distance separate, walk distance separate)? or maybe unified value?
* [X] plan a table with used points deduction
* [ ] propose a cron solution that doesn't abuse scheduler
