# Gmail OAuth2 Credentials
# This file contains your OAuth2 credentials for Gmail API access with refresh token support

# Email address for authentication
email: "your-email@gmail.com"

# OAuth2 client credentials (required for token refresh)
client_id: "your-client-id.apps.googleusercontent.com"
client_secret: "your-client-secret"

# OAuth2 access token (obtained from OAuth2 flow)
token: "ya29.a0..."

# OAuth2 refresh token (obtained from OAuth2 flow)
refresh_token: "1//09JwT..."

# Token URI for OAuth2 flow
token_uri: "https://oauth2.googleapis.com/token"

# OAuth2 scopes
scopes:
  - "https://www.googleapis.com/auth/gmail.readonly"

# Token expiry (ISO format)
expiry: "2024-09-17T13:15:00.000Z"
